export class Misc {
  id: number;
  name: string;
  category: number;

  constructor(id: number, name: string, category: number) {
    this.id = id;
    this.name = name;
    this.category = category;
  }
}