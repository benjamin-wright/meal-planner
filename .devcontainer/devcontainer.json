{
  "name": "Meal Planner Dev Container",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.shell.linux": "/bin/bash"
      },
      "extensions": [
        // Add necessary extensions here (e.g., TypeScript, ESLint, Prettier)
        // Example: "esbenp.prettier-vscode", "dbaeumer.vscode-typescript"
      ]
    }
  },
  "forwardPorts": [5173] // Port the app runs on
}